CREATE TABLE PATIENT (ID BIGINT NOT NULL, AGE INTEGER, BIRTHDATE DATE, NAME VARCHAR(255), clinic_fk BIGINT, PRIMARY KEY (ID))
CREATE TABLE CLINIC (ID BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE TREATMENT (ID BIGINT NOT NULL, TTYPE VARCHAR(31), patient_fk BIGINT, provider_fk BIGINT, PRIMARY KEY (ID))
CREATE TABLE DRUGTREATMENT (ID BIGINT NOT NULL, DOSAGE INTEGER, DRUG VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PROVIDER (ID BIGINT NOT NULL, NAME VARCHAR(255), clinic_fk BIGINT, PRIMARY KEY (ID))
ALTER TABLE PATIENT ADD CONSTRAINT PATIENT_clinic_fk FOREIGN KEY (clinic_fk) REFERENCES CLINIC (ID)
ALTER TABLE TREATMENT ADD CONSTRAINT TREATMENTpatientfk FOREIGN KEY (patient_fk) REFERENCES PATIENT (ID)
ALTER TABLE TREATMENT ADD CONSTRAINT TRATMENTproviderfk FOREIGN KEY (provider_fk) REFERENCES PROVIDER (ID)
ALTER TABLE DRUGTREATMENT ADD CONSTRAINT DRUGTREATMENT_ID FOREIGN KEY (ID) REFERENCES TREATMENT (ID)
ALTER TABLE PROVIDER ADD CONSTRAINT PROVIDER_clinic_fk FOREIGN KEY (clinic_fk) REFERENCES CLINIC (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
